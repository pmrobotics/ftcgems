DualPad Introduction
====================

.. container:: pmslide

   * combines the buttons from both gamepads into a single Gamepad instance
   * provides a "shift/alt" capability (like a keyboard) to nearly double
     the number of available buttons and controls
   * keeps track of previous gamepad iteration to simplify toggle and count
     functionality

.. toctree::
   :glob:

   ??-*

